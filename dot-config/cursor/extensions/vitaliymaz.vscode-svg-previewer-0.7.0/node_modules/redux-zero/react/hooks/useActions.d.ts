import { Action } from "../../types";
import Store from "../../interfaces/Store";
declare type ActionMap<S> = {
    [key: string]: Action<S>;
};
declare type ActionBindFunction<S> = (store: Store<S>) => ActionMap<S>;
export declare function useActions<S>(actions: ActionBindFunction<S>): ActionMap<S>;
export {};
