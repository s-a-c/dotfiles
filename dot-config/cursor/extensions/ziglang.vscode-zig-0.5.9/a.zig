var aaaa: i32 = 2;
const bbbb = 12;
// TODO FIXME something XXX
/// TODO FIXME NOTE
fn 
foo
fn foo(c: i32, d: var) void {
    a = .foo;
    b = a.cf.jffffff();
    _ = d;
    .foo = 2 []const u8 
    a: fn() void
    
    .foo => .bar
    fn @"foo bar"
    _ = @"\"foo";
    TitleCase A_ALL_CAPS T TT x
        const filename_c = ZigClangSourceManager_getFilename(c.source_manager, spelling_loc);
        ctype_t

    enum {
        u8,
    }

    .u8 

    foo.u8
    inline for
    inline       while
    error {
        Foo
        
        
        Bar
        
    }
pub extern fn ZigClangFunctionProtoType_isVariadic(self: *const struct_ZigClangFunctionProtoType) bool;
pub extern fn ZigClangFunctionProtoType_getNumParams(self: *const struct_ZigClangFunctionProtoType) c_uint;
pub extern fn ZigClangFunctionProtoType_getParamType(self: *const struct_ZigClangFunctionProtoType, i: c_uint) ZigClangQualType;
pub extern fn ZigClangFunctionProtoType_getReturnType(self: *const ZigClangFunctionProtoType) ZigClangQualType;
    async fn bar 
    noasync nosuspend async await foo bar 
    noasync nosuspend await[*c]foo
    error   .     
         
           Foo
    pub export fn entry() void
    align allowzero and asm async await break catch comptime
    const continue defer else enum errdefer export extern 
    false for if inline nakedcc noalias null or orelse packed pub fn 
    resume return linksection stdcallcc struct suspend switch test threadlocal nosuspend
    true try undefined union unreachable var volatile while callconv error 

// update syntax highlighting to master

// * noasync was renamed to nosuspend
// * nakedcc and stdcallcc will be removed soon


pub export fn entry() void {
    switch (1) {
        inline 1, 2 => {},
        inline 3...4 => {},
        inline 5 => {},
        inline else => {},
        \\foo bar
        \\jfadlsk
        \\
        \\
        \\        1231 
        \\
        \\fjdsakl 
        \\
        \



        /// fjoo
        /// ofjdsoa
        /// jfkalsfj
        //! fdjksa
        //! fadsf
        //! 
        //! 
        ///
        /// 
    }
}
\\
\\
\\


