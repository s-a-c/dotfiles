local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
    blur_radius = 30,
    color = colors.bar.bg,
    height = 40,
    padding_left = 2,
    padding_right = 2,
    position = "bottom",
})
