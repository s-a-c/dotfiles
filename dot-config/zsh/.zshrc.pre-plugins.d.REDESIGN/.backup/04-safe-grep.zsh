#!/usr/bin/env zsh
# LEGACY STUB (04-safe-grep.zsh)
# Migrated implementation now lives in: 030-grep-safe-shim.zsh
# Strategy: Option C (backward-compatible stubbing during renumbering)
# This file intentionally does nothing beyond an early return.
return 0
